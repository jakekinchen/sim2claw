# Copy to local.env (gitignored) and fill in. Prefer SSH keys over passwords.
JETSON_USER=your-jetson-user
JETSON_HOST=192.168.55.1
# Optional when not using key-based SSH:
# JETSON_PASSWORD=
REMOTE_TOGGLE=~/.local/bin/so101-teleop-toggle
PID_FILE=~/.cache/so101-teleop.pid
