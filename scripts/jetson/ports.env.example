# Install on the Jetson as ~/so101_config/ports.env
LEADER_PORT=/dev/serial/by-id/usb-REPLACE_LEADER-if00
FOLLOWER_PORT=/dev/serial/by-id/usb-REPLACE_FOLLOWER-if00
LEADER_ID=so101_leader
FOLLOWER_ID=so101_follower
