# OR119: two-material finite-object full-timeline propagation

OR119 propagates the frozen OR118 finite-object geometry and two-material
appearance across all `11` retained physical episodes and `1,210` comparison
frames. The immutable run lands inside the global pixel band at `0.837179`,
with `0.822618` pooled p10 similarity, `0.791745` motion-union similarity, and
all phase means above `0.78`.

The full acceptance contract fails. Outside-board edge F1 is `0.396353`
against the frozen `0.60` mean gate and `0.363642` against the `0.45` p10 gate;
all eleven per-episode outside-board gates fail. Board-plus-margin mean edge F1
is `0.571366` against `0.60`. The object produces a real `+0.036535`
outside-board edge gain over OR95, but only `+0.000185` global similarity,
below the frozen `+0.0004` gate.

This is a terminal-negative propagation result, not a same-video semantic,
physics, predictive, transfer, or promotion result. Reconcile the residual from
the immutable OR95 and OR119 frame rows before authoring another renderer-native
successor.
