# Brief 070 — proxy-collision V1 abort and V2 freeze

Decision: CONTINUE after commit and push.

V1 failed at source-binding validation before output-directory creation, model
loading, dynamics, or outcomes. The compact resolver inherited the base V1
temporal runner hash instead of applying the explicitly declared current
runner binding.

V2 changes only that resolver assignment. The jaw proxy-only mechanism, exact
actions, plants, variants, gates, and false physical authority remain
unchanged. Execute V2 exactly once after its freeze is pushed.
