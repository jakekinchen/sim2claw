# OR92 post-final visual-sanity residual attribution

OR91 passed every preregistered full-frame numeric gate, but a post-evaluation montage shows that this does not yet look like the same video. OR92 makes that limitation measurable without decoding either final physical video again.

It binds the six already-extracted initial/middle/terminal frame pairs, uses the stable OR81 development board polygon plus its frozen `15 px` margin, and recomputes OR83's masked tolerant-edge F1 separately inside and outside the board. A board mean of at least `0.5` with outside-board F1 below `0.4` in every sample selects robot/workcell factorization as the next mechanism.

This is post-final diagnostic evidence only. It cannot retroactively change OR91's numeric receipt, restore an untouched cohort, prove a same-video match, or promote any physics or transfer claim.
