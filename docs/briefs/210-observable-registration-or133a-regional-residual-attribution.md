# OR133A regional residual attribution and exposure audit

Use OR88 expanded-development positions `1-7` only. Explain the remaining
OR131/OR132 outside-board residual with a finite retrospective attribution
audit while changing no simulator parameter and making no fidelity or regional
progress claim. Protocol constants are explicitly outcome-informed by OR131,
OR132, and prior occupancy-image inspection.

Bind every predecessor identity, role, recording ID, frame count, renderer
group, threshold, resource cap, false claim, and authority flag before opening
pixels. Decode exactly `751` physical and `751` OR131 candidate frames, read
seven existing OR132 occupancy maps, and generate one front-most group-ID sweep
of exactly `751` frames. Every frame must contain exactly `825,548` input
triangles. Positions `8-11` and every sibling recording remain pixel-closed.

Report:

- persistent-edge geometry versus photometry diagnostics;
- per-renderer-group visible pixels, edges, and persistent overlap;
- landmark counts, bearing spans, and conditioning without fitting transforms;
- dynamic unmatched-mass shares for silhouette, nominal-light shadow,
  operator/cable, and unattributed classes;
- a metadata-only exposure ledger for the seven sibling recordings.

The admitted output directory is single-use. Determinism checks use temporary
directories. No physical image may become renderer input, texture, overlay,
mask-derived geometry, or per-episode parameter. No fit, intervention,
selection, threshold change, retry, alternate candidate, evaluation-partition
read, replay, hardware action, paid compute, promotion, or transfer claim is
allowed.

Close as exactly one of:

- `PASS_RETROSPECTIVE_REGIONAL_ATTRIBUTION_COMPLETE`, authorizing only a
  separately frozen OR133B contract citing exact OR133A receipt fields;
- `TERMINAL_ATTRIBUTION_INFEASIBLE`, authorizing only instrumentation repair;
- `TERMINAL_EXPOSURE_AUDIT_INCOMPLETE`, authorizing only exposure-ledger repair.

Every outcome keeps fidelity improvement, regional progress/resolution,
same-video semantics, held-out validity, generalization, physics/event parity,
simulator promotion, task transfer, physical transfer, and physical authority
false.

## Closeout

The one allowed run completed as
`PASS_RETROSPECTIVE_REGIONAL_ATTRIBUTION_COMPLETE`, artifact
`b0673cb231847f82ee333327c3865cb67d6f6f02521c750e5c7e8307127f678e`.
All integrity gates passed. The diagnostic route is
`operator_or_cable_scene_content` in `7/7` development episodes; geometry and
illumination routes trigger in `0/7`. Because `48.89%` of mean dynamic mass
remains unattributed and no untouched cohort remains, this is advisory evidence
for a separately frozen OR133B only, not a regional-resolution result.
