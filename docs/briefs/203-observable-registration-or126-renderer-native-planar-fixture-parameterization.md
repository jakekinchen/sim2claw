# OR126 renderer-native planar-fixture parameterization

Parameterize only OR125's fully observed component-4 fixture. Decode the same procedural dictionary entry and four image corners in all seven development and four corroboration initial frames. Freeze development-median corners, a procedural 8x8 binary cell pattern generated from the dictionary rather than physical pixels, and model-coordinate plane intersections under the existing frozen camera/workcell model. Reprojection is self-consistency, not physical metric calibration. No render or video is emitted.
