# OR117: finite linear object appearance failure attribution

OR117 reads only the immutable OR116 development montage and receipt. The
candidate-minus-baseline footprint is partitioned using the frozen terminal
center/radius. The shaft physical median is `[150,148,145]`; the terminal is
`[55,48,43]`, separated by `171.55` BGR units. Signed physical-minus-candidate
grayscale residuals are opposite (`+31.10` shaft, `-44.57` terminal).

All gates pass for a two-material appearance factor. This does not validate a
two-material renderer candidate and does not reopen OR116 validation.
