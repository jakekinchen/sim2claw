# OR133B boundary-residual spatiotemporal falsification

OR133A's integrity passed, but its `operator_or_cable_like` name exceeded its
measurement. The implementation counted every non-shadow 8-connected component
touching any image border. It did not test entry side, texture, topology,
temporal coherence, cable structure, or actor presence. OR133B therefore uses
the semantic-free name `boundary_connected_nonshadow_residual` and authorizes
no intervention.

Use OR88 expanded-development positions `1-7` only. Decode the exact `751`
physical and `751` OR131 candidate frames, read the same seven OR132 occupancy
maps and retained physical sample series, and rerender the exact `751` OR131
triangle streams once with the already-proven equivalent ID buffer. Every frame
must contain `825,548` triangles. Positions `8-11` and sibling pixels remain
closed.

Reproduce the OR133A unmatched residual and shadow exclusion exactly. Enumerate
named border sides, component size/bbox/centroid, per-robot silhouette distance
bands, light-side/dark flags, and deterministic adjacent-frame tracks using
overlap under the frozen `3x3` tolerance. Report side-specific mass, coherent
track coverage, arm-adjacent/interior shares, and fixed normalized-timestamp
montages.

For each episode, compare boundary mass at lags `-3..3` frames with physical
follower-speed norm and leader-innovation norm. A coupling branch requires
absolute Pearson correlation at least `0.30` and greater than the 95th
percentile of 40 preregistered circular-shift maximum correlations whose
minimum circular displacement is five seconds. At least `6/7` episodes must
pass. If both signals qualify, close as shared-action/phase confounded. If
neither qualifies, close as retained-evidence unidentifiable.

There are zero renderer/intervention degrees of freedom, fits, selections,
threshold changes, retries, candidate renders, simulator replays, hardware
actions, or paid-compute operations. Physical pixels never enter the renderer,
texture, geometry, state, or output image. No outcome authorizes geometry,
screen-space content, a leader-slaved proxy, operator/cable identity, regional
progress, held-out validity, promotion, physics/event parity, or transfer.
