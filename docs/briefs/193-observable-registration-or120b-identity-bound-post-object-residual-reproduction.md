# OR120B: identity-bound post-object residual reproduction

OR120B repairs OR120's execution-identity gap under new immutable paths. Its
contract binds the implementation and focused test hashes before the one-run
evaluation. The run reuses OR97's exact factorization over the same `11`
physical and OR119 videos, matches all quarantined OR120 numeric rows, preserves
OR97 physical occupancy identity, and emits `11` eight-panel residual maps.

Persistent outside-board occupancy F1 is `0.345013`, versus `0.571429` dynamic.
The mean deficits to `0.60` are `0.254987/0.035281`, a `7.2273x` ratio, and the
stricter `3x` per-episode persistent-dominance rule passes `9/11` episodes.

The authoritative successor is post-object persistent-static spatial
decomposition. The original OR120 receipt stays quarantined and is not
promoted. No specific scene object, same-video semantic match, prediction,
physics, event parity, transfer, or simulator promotion is established.
