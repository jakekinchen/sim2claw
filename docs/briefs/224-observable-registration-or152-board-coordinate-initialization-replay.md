# OR152 board-coordinate initialization replay

OR151 and its independent Reviewer confirmed that OR34 copied a pawn world XY
across incompatible scene geometry. OR152 therefore freezes exactly one new
observation-conditioned replay. It preserves OR34's 531 raw measured robot
rows, timestamps, interpolation, OR18 model and solver, range union, contact
and object parameters, support Z, upright quaternion, post-action settle, and
task evaluator. Its only change is selected-pawn initial XY, transported from
the retained file/rank board coordinate through OR18's board basis.

The outcome is write-once. There is no fitting, search, retry, action repair,
IK, offset assistance, clipping, retiming, latch, endpoint injection, hardware,
or paid compute. Advancement is prospectively defined as at least one frozen
task-consequence metric improving materially—or one unchanged task gate
flipping false to true—while selected-pawn contact remains present. Every
improvement and regression must be reported. Full simulator task success still
requires all unchanged OR34 gates.

This is an observation-conditioned simulator diagnostic, not action-only or
physical transfer, physical task evidence, calibrated world registration,
physics fidelity, simulator promotion, or transfer proof.
