# OR123: renderer-native planar-array failure attribution

OR123 read only the immutable OR121 residual maps and OR122B development
montage. It performed no footage decode, render, fit, search, retry, replay, or
hardware action.

The five rendered centerlines cover `97.44%` of their expected image-space line
support and improve the changed-pixel residual by `3.20` intensity units on
average, with `68.90%` of changed pixels improving. They nevertheless cover
only `31.28%` of OR121's `911`-pixel persistent consensus, leaving `626` pixels
uncovered. This admits the narrow conclusion that the five centerlines are
spatially insufficient; material response is not the primary miss.

The tabletop-plane claim remains unresolved. Exact re-projection is guaranteed
by the construction that back-projects physical-image endpoints to a chosen
plane, and the audit contains multiple structured residual components. Before
adding dense renderer geometry, use retained temporal footage to determine
whether the uncovered structure is workcell-static or robot-/actor-attached.
No physical object identity, physics fidelity, transfer, or promotion is
claimed.
