# Brief 068 — canonical temporal result and contact-witness freeze

Decision: CONTINUE after the scoped result/freeze commit is pushed.

## Official result

The post-freeze V2 replay ran exactly once and is a preserved negative.
REAL->SIM is `0/2`, SIM->REAL is `0/2`, and no one of the `40` fixed
case/plant/reset outcomes passed. Selected contact passed `40/40`, while
no-lift passed `0/40`; progress passed `23/40` and exclusion displacement
passed `33/40`. Requested, mapped, sent, and applied trace integrity checks
pass on all eight case/plant paths.

## Diagnostic successor

Freeze one nominal-only simulator witness over the same four actions and the
same direct-target and diagnostic `0.11 s` ZOH paths. It may report first
jaw/pawn contact, planar motion, vertical rise, geometry pairs, contact height,
vertical-normal magnitude, force, and board support. It changes neither action
nor plant.

## Stop

Do not treat the witness as physical calibration, a simulator promotion, or a
license to tune an outcome. Preserve the negative and choose or reject only
one prospectively declared mechanism after the immutable witness exists.
Physical authority remains false.
