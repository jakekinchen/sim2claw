# OR128 renderer-native planar-fixture full-timeline propagation

Append OR127's frozen 128-triangle fixture to the existing OR119 full-mesh stream before the already-frozen finite object. Reuse the exact eleven traces, 1,210 timestamps, camera, workcell transforms, response, native rasterizer, metrics, and same-video thresholds. Emit one candidate video per episode. The wrapper records its own contract/implementation/test identities and preserves the OR119 evaluator result without opening fit, selection, retry, replay, hardware, or paid compute.
