# OR53 — Existing simulator trace corpus against the footage constraint

Decision: `CONTINUE`

Evidence anchor: OR52 establishes a persistent image-plane enclosure proxy
during the successful physical carry, while OR51's isolated numeric-success
trace contains only moving-jaw contact.

## Required outcome

Audit every already-run fixed-pad longitudinal candidate from OR38, OR49, and
OR50 against the same five physical preterminal gates. Quantify whether the
existing simulator corpus contains any candidate that combines bilateral named
contact, physical motion/support timing, and sample-260 uprightness. Do not run
simulation, generate candidates, refit, or select a promotable parameter.

## Frozen population and gates

- OR38: all `9` candidates.
- OR49: all `19` candidates.
- OR50: all `25` candidates.
- Require exact source receipt hashes and exactly `53` unique candidate rows.
- Preserve the existing gate meanings: contact in samples `228–232`, no motion
  above 1 mm before `247`, support loss in `247–260`, bilateral contact in
  `247–270`, and tilt at sample 260 no more than `10 deg`.
- Report terminal outcome only after the preterminal gate audit; terminal
  outcome cannot rank or promote a candidate.

## Terminal rule

If no candidate passes all five gates, close the already-sampled single
fixed-pad-longitudinal coordinate family as insufficient. Distinguish a
bilateral-but-early branch from a timing-correct-but-unilateral branch. Do not
claim the unsampled continuum is mathematically exhausted.

## Claim boundary

This is a retrospective, quarantined, zero-new-replay audit. It cannot identify
metric pad geometry, promote a simulator, validate transfer, or prove physical
contact force or bilateral physical contact.
