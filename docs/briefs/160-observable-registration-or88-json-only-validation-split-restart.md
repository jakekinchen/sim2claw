# OR88 JSON-only validation split restart

OR87 rejected the frozen candidate on tolerant-edge F1 while leaving the original four-episode evaluator-heldout cohort unopened. OR88 freezes a new, non-retroactive development/validation/final-heldout partition before any additional fitting.

The original split positions `1-4` and the now-opened positions `5-7` become expanded development. Original positions `8-9`, chosen by the already-frozen ascending recording-ID hash order, become fresh validation. Positions `10-11` remain the final evaluator-heldout. This card may stat and hash declared files but may not decode a physical video, render a frame, fit a parameter, or open either new evaluation role.

The next card may fit one shared, monotone, non-spatial response family on expanded development only. Fresh validation and final held-out must remain unavailable to selection.
