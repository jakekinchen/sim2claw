# OR125 workcell-static component surface identification

Use one complete planar-fiducial witness in each retained initial frame to test whether the dominant OR124C component subset shares OR121's frozen `50/140 degree` rectilinear axes and lies within a fixed `60 px` support neighborhood. Individual marker identity is discarded. The card may select only a separate static planar-fixture family or indeterminate; it cannot fit geometry/material, render, replay, or claim a named object, depth, physics, transfer, or promotion.
