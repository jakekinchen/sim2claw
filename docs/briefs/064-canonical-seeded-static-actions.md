# Brief 064 — canonical seeded static actions

Decision: CONTINUE. Evidence anchor: 100.

## Slice

Freeze a bounded canonical-runtime action compiler whose exact row zero maps
to the fresh torque-off follower pose. Enumerate every reset pawn with an
empty orthogonal neighbor over one contact offset, two contact heights, and
two stroke lengths.

## Acceptance

- Canonical registration and the readiness rejection remain bound.
- Every action is C-contiguous little-endian float64 at 40 Hz.
- Row zero inverse-maps to the fresh physical anchor within `1e-12`.
- No clipping, repair, runtime IK, runtime offset, corrective suffix, or
  post-freeze retiming occurs.
- IK residual is at most `4 mm`.
- All rows remain inside calibrated limits and gateway rates.
- Static kinematics observe selected-pawn contact and no new disallowed robot
  contact.
- Source and terminal remain at least `20 px` inside the C922 image.
- Four distinct families freeze, alternated two per direction.

## Stop

Reject closes this grid without dynamics. Pass authorizes a separate frozen
direct-target plus `0.11 s` ZOH simulator consequence gate only. Camera,
gateway, serial, physical packets, torque, contact motion, and task attempts
remain unauthorized.
