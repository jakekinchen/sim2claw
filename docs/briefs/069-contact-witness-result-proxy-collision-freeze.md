# Brief 069 — contact-witness result and proxy-collision freeze

Decision: CONTINUE after the scoped result/freeze commit is pushed.

The exact-action witness ran once. Every nominal rise follows jaw contact.
Six of eight case/plant paths first contact the pawn above `50 mm` from its
root, with absolute vertical normal components near `0.78--0.81`. The first
colliders are enabled high-detail jaw collision meshes; named jaw collision
primitives are present in the same reviewed model.

Freeze one challenger: disable the three enabled left-jaw collision meshes and
retain the named collision primitives. Preserve exact actions, direct/ZOH
plants, five reset variants, task gates, pawn/board geometry, mass, friction,
damping, timing, actuation, and false physical authority.

A pass is still diagnostic. It cannot calibrate physical geometry or promote a
simulator without subsequent evidence and untouched validation.
