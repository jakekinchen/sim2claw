# OR116: renderer-native finite linear workcell object reconstruction

OR116 places one development-frozen shaft and bounded terminal on the exact
transformed tabletop plane and renders `348` real triangles in the OR95 shared
z-buffer. It materially improves outside-board edge F1 (`+0.044163`) and local
object-ROI edge F1 (`+0.730876`) on all seven development rows without changing
board edges.

The card is nevertheless terminal-negative because mean full-frame similarity
improves only `+0.000157`, below the frozen `+0.001` gate. Validation remains
sealed. The montage indicates a one-material appearance failure: the shaft is
too dark while the terminal is too light. The next card may attribute that
split but may not relax OR116's gate or treat its structural win as acceptance.
