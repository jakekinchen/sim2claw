# OR124: planar-array residual motion-ownership attribution

OR124 produced an internally digest-valid receipt that reports workcell-static
motion ownership on development and corroboration. The receipt is not admitted:
its contract, implementation, and test hashes all differ from the final files,
and the receipt predates every final identity.

The apparent static result is advisory only. It does not authorize dense
renderer geometry or a named physical object. Reproduce the same bounded
retained-footage ownership question under a new immutable contract, source,
test, and output path. Do not overwrite OR124 or restore old files after the
fact.
