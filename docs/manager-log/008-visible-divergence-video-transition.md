# Manager intervention 008 — visible-divergence video transition

Decision: `CONTINUE`

Evidence anchor: `100`

The OR23 terminal result closes causal parameter identification from retained
physical contact evidence. It does not prohibit a visualization-only trace
comparison. The owner explicitly requested the first visible divergence and a
nearly identical synchronized physical-versus-physics video.

OR26 therefore opens a separate, bounded lane:

- replay no dynamics and change no action, state, contact, or object parameter;
- render the immutable OR21 trace at the original 531 source timestamps;
- use the retained C922 board projection and measured 324 mm playing surface;
- allow one explicit board-plane display homography so static board geometry
  can be compared in a shared image frame;
- remove static appearance differences from the motion metric with
  frame-to-frame differences;
- report board, robot, pawn, and timing divergence separately;
- preserve false global-mapping, task-success, and transfer authority.

The published Phase-A comparison's third lane was intentionally blank because
the earlier proof contract rejected action-frozen physics. OR19 and OR21 now
provide an immutable exact-action physics trace, so leaving that lane blank is
stale product behavior rather than an evidence boundary.
