# Reviewer message 086 — OR6 pass; activate OR7

Decision: CONTINUE. Evidence anchor: 100.

OR6 promotes a task-bounded aperture-mapping candidate while global mapping
remains false. The single fitted value is `0.0494823931 rad`. Aperture RMS
improves from `21.259` to `1.848 px` on six fit poses and from `24.045` to
`3.828 px` on four no-refit validation poses. All midpoint-regression,
parameter-bound, identity, and model-joint-range gates pass.

Activate OR7. Freeze a new one-run experiment that reuses the exact immutable
C6 requested, gateway-sent, identified-applied, timestamp, row-order,
initialization, plant, contact, object, and evaluator inputs. Change only the
candidate gripper zero offset. Report selected-jaw contact count and first
sample, first pawn motion, catastrophic displacement if any, final task gates,
and exact change versus C6. Natural contact only; no marker, latch, endpoint,
state, action, camera, timing, or support assistance.
