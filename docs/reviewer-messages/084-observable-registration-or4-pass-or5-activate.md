# Reviewer message 084 — OR4 pass; activate OR5

Decision: CONTINUE. Evidence anchor: 100.

OR4 preserves every exact C6 action and timestamp and localizes the earliest
accepted divergence to selected-jaw enclosure. The physical episode has
definite enclosure by sample `232`; the simulator has zero selected-jaw
contacts there and does not move the pawn by more than `1 mm` until sample
`386`, a `154`-sample or `7.697 s` gap. At enclosure, the physical and
plant-applied gripper differ by only `0.234 deg`, so action timing is not the
selected first mechanism.

Activate OR5 on `jaw_aperture_and_gripper_geometry_mapping`. Use only the
already nonsealed static jaw annotations to test identifiability and declare
the smallest finite parameterization. Do not use C6 task outcome, change
actions, fit contact material, or approve global mapping. The declared
mechanism must preserve the immutable OR1 camera and report fit and untouched
validation separately.
