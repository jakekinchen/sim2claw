# Reviewer Message 003: LLM Corrective Runtime

Decision: `CONTINUE`

Evidence anchor: `100`

## Finding

T01--T06 satisfy their scoped gates. The new contracts fail closed on held-out
or physical intake, privileged observations, raw joint commands, excessive
motion, identity mismatch, IK/rate/collision failures, unsupported posterior
parameters, seed leakage, and proposal-score authority escalation. The real
MuJoCo fixture proves exact branch restoration and bounded execution.

## Required next slice

Complete T07 with a canonical source-episode writer or generator hook that:

1. preserves the exact failed prefix;
2. inserts the compiled intervention actions at the declared branch;
3. records fresh synchronized observations and privileged evaluator state;
4. supplies a declared geometric continuation through termination;
5. passes the unchanged full source evaluator; and
6. proves LF-09 receives suffix rows only.

Do not weaken the existing admission contract or use the branch-only receipt
as a substitute. Add a regression that rejects an intervention artifact whose
compiled actions do not match the corrective episode rows.
