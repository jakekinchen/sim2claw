# Reviewer decision 048: Q15 Fable final reconciliation

Date: 2026-07-27

Model: Claude Fable 5 (`claude-fable-5`)

Thread: existing Claude Desktop conversation
`Sim-to-real transfer evaluation`

Decision: `ACCEPT`

Branch reviewed:
`codex/geometric-microtransfer-20260727` at `ee7ef46`, local equal to origin.

Mode: read-only advisory. Fable modified no repository file and touched no
camera, gateway, robot, or paid compute.

## Independent reconciliation

Fable verified the correction commits and hashes, re-extracted the C922 apex
at `10.38 s`, inspected the sealed Pi apex frame, and independently confirmed:

- the original Q03 receipt is byte-identical and its B7/`164.353128 mm`
  score is preserved only as invalid provenance-labeled history;
- C922 occlusion, high-hover parallax, missing metric extrinsics, the Pi
  field of view, and the D405 orientation make an exact physical square
  unadjudicable from the sealed evidence;
- refusing the v4-FK A3 and raw-center A2 substitutions avoids circular
  held-out labeling;
- the corrected v4 status is indeterminate, not admitted or rejected;
- held-out open count remains one and the F1 trigger is unsupported;
- the Q05 upper-bound proof is sound:
  `sqrt(2) * 44.45 = 62.861793 mm < 88.9 mm`;
- Q06's corrected proof class
  `terminal_preregistered_contract_infeasibility_without_physical_attempt`
  is supported and is not a safety/F3/task/transfer event;
- terminal package/viewer hashes verify, with REAL→SIM `0/0`, SIM→REAL
  `0/0`, total `0/10`, and no action hashes;
- the branch was pushed through `ee7ef46`.

Fable found no remaining required-criterion defect that must reopen a card.

## Strongest supported result

A receipted evaluator-integrity chain: the categorical board-side diagnosis
was independently reproduced; v4 reduced immutable C2 diagnostic clearance
from `312.3` to `75.6 mm` without changing action bytes; the infeasible
preregistered contract failed closed with zero robot motion; and the
post-review correction preserved originals, refused circular rescoring,
opened no new held-out data, and moved no threshold.

## Weakest residual limitation

V4 metric registration is permanently indeterminate on the existing split:
the single held-out open is spent and the sealed cameras cannot identify its
physical square. There is no admitted registration and no task-transfer
result. Any successor campaign requires a separately authorized,
camera-adjudicable held-out episode plus a new feasible evaluator with
freeze-time reachability and clearance checks.

## Non-blocking notes

- Future corrected ignored-run receipts should use versioned siblings rather
  than in-place overwrite.
- Immutable historical logs retain the superseded B7-fail/safety wording; the
  queue explicitly supersedes it.
- A successor must not inherit the unsupported F1 consequence gate.
- Pi wording could more precisely say the board area beneath the hover is out
  of frame even though jaw tips appear at or inside the edge; this does not
  change the unadjudicability decision.

Final Fable wording: Q00-Q14 closeout is honest and evidence-complete as
corrected; Q15 may be marked complete.
