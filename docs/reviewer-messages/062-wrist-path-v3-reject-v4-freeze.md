# Reviewer 062 — wrist/path V3 reject and V4 freeze

`CONTINUE` after the scoped transition is committed and pushed.

The V3 negative is immutable. V4 is static-only, finite, and changes only the
precontact segment. It does not weaken collision, contact, gateway, camera,
family, or authority gates.
