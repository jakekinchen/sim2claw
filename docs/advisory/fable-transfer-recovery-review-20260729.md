# Fable Transfer-Recovery Review

Status: `ADOPT_RP00_SIMULATION_ONLY_CERTIFICATE`

Date: `2026-07-29`

Reviewer: Claude Fable 5, effort `High`, existing project thread

Reviewed branch and commit:
`codex/bidirectional-transfer-goal-loop-20260728` at `b60cf97`

Review mode: read-only repository inspection. Fable changed no files and
opened no camera, gateway, serial, hardware, or paid-compute authority.

## Owner objective

Recover genuine nonzero proof for:

- REAL->SIM task transfer, currently `0/0`;
- SIM->REAL task transfer, currently `0/0`;
- physical task attempts, currently `0/10`;
- a task-scope physical/model mapping; and
- a bounded policy-ranking pilot only if enough paired physical samples exist.

The review was forbidden from weakening collision/evidence gates, relabeling
the wrist correspondence as task transfer, or inferring policy prediction
from zero physical samples.

## New falsifiable hypothesis

Fable reported that the CC03K boundary is valid at the exact
`99.472527 deg` elbow lock but does not establish a global workspace
boundary.

Its read-only pose-level sweep reported:

| Elbow lock | Strict-clean poses below 60 mm at pawn radii | Lowest site |
|---|---:|---:|
| `99.47 deg` | `0` | unavailable |
| `95 deg` | `70` | `41.3 mm` |
| `90 deg` | `608` | `20.3 mm` |
| `85 deg` | `1,007` | `20.7 mm` |
| `75--80 deg` | about `1,350--1,480` | about `20 mm` |

This is advisory pose-space evidence, not a route-level action admission.
The first accepted action is therefore only a prospective route-level
certificate over elbow locks `{97, 95, 93, 91, 90, 88} deg` using the exact
CC03K family universe and unchanged gates.

## Rejected alternative

Fable mined `3,023` physically visited poses from `11` joint-sample files.
It reported maximum physically evidenced modeled overlaps of about:

- `3.24 mm` for lower-arm/shoulder;
- `2.15 mm` for upper-arm/wrist; and
- `1.82 mm` for shoulder/wrist.

The current contact-band poses would require about `3.3--10.9 mm`.
The corpus therefore does not authorize a collision allowance expansion.
CC03K remains immutable and correct at its original lock.

## Adopted finite path

1. RP00: one simulation-only parking-target certificate.
2. RP01: freeze a contact-impossible high-clearance parking transaction only
   if RP00 passes.
3. RP02: one camera-enclosed physical parking transaction only under new,
   explicit physical authority and reviewed abort/cleanup gates.
4. RP03: rerun and freeze task actions at the achieved lock.
5. RP04: REAL->SIM attempt, with at most three task attempts.
6. RP05: distinct SIM->REAL attempt, with at most three task attempts.
7. RP06: approve only the task-bounded mapping supported by the successful
   slice; global mapping remains unclaimed.
8. RP07: run a policy-ranking pilot only if at least four paired cases exist.
9. RP08: package exact receipts, denominators, Studio evidence, and claims.

RP00 is adopted. RP01--RP08 remain closed until their predecessor gates pass.

## Claim boundary

The review creates a new static hypothesis, not a capability claim. It does
not alter the existing `0/0`, `0/0`, and `0/10` ledgers, approve a mapping,
authorize physical motion, or make policy ranking predictive.
