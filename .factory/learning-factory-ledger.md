# Learning Factory Execution Ledger

> **Current proof correction (2026-07-19):** The entries below recorded the
> first controller-scaffold run. Their M0-through-M8 completion language is
> superseded by the gap audit in
> `docs/briefs/008-learning-factory-buildout-gap-audit.md`. Literal synthetic
> stage outputs and caller-supplied verdict fields prove orchestration
> mechanics only. Real deterministic stage integration remains open.

## 2026-07-19T10:34:46Z — superseded M0 through M7 scaffold record

Updated at: 2026-07-19T10:34:46Z

Branch / HEAD: `codex/task-orchestrator-plan` / `13999e9`

Selected project: `configs/projects/pawn_rank12_reachable_bg_hackathon_v1.json`

Current milestone: M8 clean-runtime acceptance and closeout

Current factory stage: LF-05 — Freeze replay-ready splits

Stage status: `blocked`

Completed:

- M0: exact LF-00 through LF-13 graph, typed statuses, authority-bound stage
  results, deterministic navigation cards, and negative contracts.
- M1: project-scoped controller, atomic attempt/result receipts, exclusive
  stage leases, dependency-aware next/range execution, hash-aware reuse and
  resume, explanation output, and coherent CLI.
- M2: physical project inspection through canonical-source inventory and a
  complete deterministic source-to-dataset fixture with explicit rejections
  and zero held-out/rejected training rows.
- M3: immutable training-candidate capture, separate evaluator scorecard, a
  task-bound historical narrow ACT evidence binder, and an explicit prohibition
  on relabeling rook-lift evidence as a B--G policy.
- M4: bounded calibration experiment, split validation, sensitivity gate,
  before/after fidelity comparison, and simulated-success-independent verdict.
- M5: content-addressed counterexample normalization, atomic registry merge,
  conflict detection, disposition, and held-out correction guard.
- M6: bounded pose/layout cousin compiler plus recursive fixture path through
  replay, dataset admission, training, independent evaluation, counterexample,
  and promotion state.
- M7: read-only Studio API and responsive stage rail with blockers, evidence,
  owners, actions, and copyable resume commands.

Evidence:

- Synthetic full-loop run SHA-256:
  `ed82383a9fdc5b43b7825ad667ae9b053e679a8892ea5ac52472f8c9a362e08a`.
- Physical bounded run SHA-256:
  `b8a3b60dc5845dbd56929952a30533f5705ae299a1237b1f144c5e8073b81d36`.
- Narrow ACT evidence SHA-256:
  `f43e7ad4b5dc2f5bf4967608f18bc89608b79267e812a09b2a9748cf8db3aa6d`.
- Studio desktop, expanded-blocker, and mobile screenshots live below the
  ignored `.playwright-cli/` evidence root for this checkout.

Tests: `uv run pytest -q tests/test_learning_factory.py` — 16 passed before
M8 broad regression.

Artifacts / receipts:

- `outputs/learning_factory/fixture-20260719T103133Z-f33d2c1b/fixture_evidence.json`
- `outputs/learning_factory/narrow_act_evidence.json`
- `runs/learning-factory/projects/pawn_rank12_reachable_bg_hackathon_v1/stages/LF-05/attempts/20260719T102851Z-7e337f3f49/stage_result.json`

Remaining:

- Run full repository tests, build/lock/compile/JSON/link/ignore/diff checks.
- Reconcile any regressions without overwriting concurrent Task Orchestrator
  work.
- Currentize the task document and publish a final evidence map.

Blockers:

- The current physical catalog contains zero exact joint/timing replay-ready
  episodes.
- The physical joint transform remains
  `provisional_unapproved_range_audit_blocked`.

External owner action: approve a correct joint transform and provide or repair
at least the declared minimum exact-replay cohort; then rerun LF-05 with the
copyable command emitted by `factory-explain` or Studio.

Paid resources: none created, started, or used by this learning-factory task.

Next step: execute M8 verification and record the final result without widening
the physical or B--G learned-policy claim.

## 2026-07-19T10:37:25Z — superseded M8 scaffold verification

Updated at: 2026-07-19T10:37:25Z

Branch / HEAD: `codex/task-orchestrator-plan` / `13999e9`

Selected project: `configs/projects/pawn_rank12_reachable_bg_hackathon_v1.json`

Current milestone: M8 scaffold verification only; full buildout open

Current factory stage: LF-05 — Freeze replay-ready splits

Stage status: `blocked` for the selected physical campaign; controller
scaffold verified, workflow implementation incomplete

Completed:

- Targeted learning-factory plus NemoClaw project regression passed: 87 tests.
- Full repository regression passed: 412 tests and 324 subtests.
- `uv lock --check`, Python compile, all config/calibration JSON parses, Bash
  syntax checks, `uv build`, documentation links, ignore policy, credential
  pattern scan, and `git diff --check` passed.
- A fresh locked virtual environment in an isolated source snapshot passed 16
  focused tests and a 14-stage fixture run.
- Studio was inspected in a real browser at desktop and mobile widths; the
  expanded LF-05 card showed the exact evidence, blockers, owner, action, and
  copyable resume command.

Evidence:

- Isolated fixture run SHA-256:
  `c9707cd2f09be34e45c662b7f2eb14415b69a3bf4d688fee1d5edc8471f0dba9`.
- Final current-code physical bounded run SHA-256:
  `9f01fc05724320d8ce734d0d4e73175d06409896ba3f0d8c4a4fce9c3344521e`.
- Isolated acceptance root:
  `/tmp/sim2claw-learning-factory-acceptance.1bd73d`.
- In-place fixture, physical blocker, and ACT hashes remain as recorded above.

Tests: all applicable checks passed.

Artifacts / receipts: generated evidence remains ignored under `outputs/`,
`runs/`, `.playwright-cli/`, `dist/`, and the isolated `/tmp` acceptance root.

Remaining: P0 through P8 build cards in the gap audit, including substantial
hardware-free controller hardening and real LF-01-through-LF-13 component
integration.

Blockers:

- Zero exact joint/timing replay-ready physical episodes.
- Joint transform `provisional_unapproved_range_audit_blocked`.

External owner action: approve the correct transform and repair/provide the
minimum exact-replay cohort, then resume LF-05. Do not open held-outs or train
before that gate passes.

Paid resources: none created, started, or used; no Brev inventory was touched.

Next step: use `factory-status`, Studio, or `factory-explain --stage LF-05` to
navigate the blocked campaign when new physical evidence arrives.

## 2026-07-19 — Buildout acceptance ledger initialized

Authority: `docs/briefs/008-learning-factory-buildout-gap-audit.md` and the 18
acceptance criteria in `docs/autonomous-workflow/goal-loop-learning-factory.md`.

| Criterion | State | Current evidence or missing proof |
| ---: | --- | --- |
| 1 | `partial` | Exact graph exists; full code/runtime/artifact identities remain P1 |
| 2 | `passed` | Coherent inspect/status/next/range/resume/explain CLI exists |
| 3 | `partial` | Atomic receipts exist; content-addressed artifacts and complete implementation identity remain P1 |
| 4 | `partial` | Blocked dependencies stop; robust structured failures and stale-lease recovery remain P1 |
| 5 | `passed` | No secondary LLM provider is required |
| 6 | `open` | Real source-to-dataset APIs are not chained |
| 7 | `open` | False-lineage local ACT profile quarantined; replacement must consume LF-09 data |
| 8 | `open` | Arithmetic helper exists; real before/after execution is absent |
| 9 | `open` | No independently executed calibration admission adapter |
| 10 | `partial` | Schema/deduplication helpers exist; real trace routing and correction evidence absent |
| 11 | `partial` | Bounds helper exists; no compiled/replayed cousin generator |
| 12 | `open` | Recursive edges are metadata only |
| 13 | `open` | Promotion helper trusts caller fields instead of revalidating producer receipts |
| 14 | `partial` | Read-only Studio rail exists; artifact/version drilldown remains |
| 15 | `open` | Runtime skill-package and counterexample-return interfaces are not joined |
| 16 | `partial` | Scaffold tests passed; real-component integration and final clean-clone acceptance remain |
| 17 | `partial` | Current generated roots are ignored; final artifact-store and clean-clone audit remain |
| 18 | `not_applicable` | No Brev resource has been used for this buildout |

Current milestone: P0 proof correction in progress, then P1 controller and
adapter hardening.

Paid resources: none created, started, or used.

## 2026-07-19 — Post-audit hardware-free buildout

Updated at: 2026-07-19T11:47:00Z

Branch / base HEAD: `codex/task-orchestrator-plan` / `13999e9`

Selected project: `configs/projects/pawn_rank12_reachable_bg_hackathon_v1.json`

Current milestone: P8 committed clean-clone acceptance

Current factory stage: LF-05 — selected physical campaign

Stage status: `blocked` on external physical evidence; hardware-free P0 through
P7 implementation complete

Completed:

- P0 proof correction and false-lineage quarantine.
- P1 typed adapters, full component identities, immutable artifacts, terminal
  attempts, recoverable leases, and campaign generations.
- P2/P3 real intake, twin validation, replay/split, sysid, and separate
  calibration evaluation adapters.
- P4 real strict source replay/evaluation, verified cousin lineage, immutable
  ACT/GR00T datasets, and exact rejection artifacts.
- P5 dataset-consuming 61-D ACT training and separate CPU/fp32 online
  consequence evaluation.
- P6 trace-native counterexamples, guarded corrections, and executable child
  generations.
- P7 independent promotion/rejection and hash-guarded orchestrator handoff.
- Studio immutable-artifact drilldown and generation history.

Acceptance criteria:

| Criterion | State | Current evidence |
| ---: | --- | --- |
| 1 | `passed` | Exact project/graph/contract/component/artifact identities |
| 2 | `passed` | Inspect/status/next/range/resume/explain plus generation-aware recurse |
| 3 | `passed` | Atomic content-addressed outputs and stage-specific runtime identity |
| 4 | `passed` | Fail-closed dependencies; LF11 terminal-negative may enter LF12 only for failure routing |
| 5 | `passed` | No secondary LLM configuration |
| 6 | `passed` | Real source evaluator to immutable ACT/GR00T dataset; zero held-out/rejected rows |
| 7 | `passed` | Exact LF09 dataset to atomic checkpoint to separate evaluator; no self-promotion |
| 8 | `passed` | Real sysid fixture before/after comparison; policy probe excluded from admission |
| 9 | `passed` | Separate calibration evaluator process owns admission |
| 10 | `passed` | Trace-native stable registry and byte-validated corrective-suffix path |
| 11 | `passed` | Bounded training-only curriculum, verified transforms, exact rejection receipt |
| 12 | `passed` | Generation-1 child verdict with generation-0 evidence unchanged |
| 13 | `passed` | Independent promotion process revalidates every producer receipt |
| 14 | `passed` | Read-only Studio rail, blockers, history, and artifact drilldown |
| 15 | `passed` | Hash-guarded skill package out; typed authenticated counterexamples back |
| 16 | `partial` | Focused/full tests and compile/diff pass; committed clean-clone checks pending |
| 17 | `passed` | Runtime outputs remain ignored; source contains contracts/tests only |
| 18 | `not_applicable` | No Brev or paid resource used |

Evidence: `docs/run-logs/2026-07-19-learning-factory-buildout.md`.

Tests: 443 passed, 328 subtests passed before final documentation/publisher
additions; final rerun pending.

Remaining: lock/build/schema/link/ignore audit, scoped commit, genuine Git clone,
and clean-clone focused/full verification.

Blockers: physical LF-05 inputs and B--G compatible source/evaluation data only.

External owner action: approve/recollect the exact physical evidence listed in
the run log, then resume LF-05. Do not relabel the upper-side mechanism source.

Paid resources: none created, started, or used. No Brev inventory was touched.

Next step: finish P8 from committed Git bytes.

## 2026-07-19 — P8 committed clean-clone acceptance

Accepted code HEAD: `416eb77`

Clean clone: `/tmp/sim2claw-release-clean-p64ZFe`, created with
`git clone --no-local` from the live repository.

Completed:

- `uv sync --frozen` created a fresh Python 3.12 environment from the lock.
- The full suite passed: 446 tests and 328 subtests in 130.69 seconds.
- The production-backed LF-00-through-LF-13 component campaign passed its
  orchestration assertions and retained the expected LF-11 terminal-negative
  and LF-13 authenticated rejection.
- The same campaign independently replayed a strict corrective episode,
  matched evaluator identity/measurements/gates and the exact branch state,
  admitted 561 corrective-suffix rows, and authorized zero failed-prefix rows.
- `uv lock --check`, source/wheel builds, Python compile, 69 tracked JSON
  parses, tracked Bash syntax, generated-root ignore checks, clean tracked
  status, and `git diff --check` passed.
- The test provisions its two ignored project-bundle directory mounts, so the
  campaign does not rely on residue from the development checkout.

Acceptance criteria 1 through 17: `passed`.

Acceptance criterion 18: `not_applicable`; no Brev or other paid resource was
created, started, or used.

Remaining blockers are external campaign evidence only: the selected physical
project remains truthfully blocked at LF-05, and no successful B--G learned
policy or physical authority is claimed.
